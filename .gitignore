node_modules/
.next/
out/
*.log
.env*
.DS_Store
next-env.d.ts

# Caché incremental de TypeScript: lo reescribe cada `npx tsc`, así que
# versionado ensucia todos los diffs sin aportar nada. Se regenera solo.
*.tsbuildinfo
